Inside this directory is some XML that needs to be present in web.xml in order 
for DispatcherServlet and all of its extensions to function. They are here 
in order to be packaged into End-User distributions of the library/framework 
via the build script or serve as backups.

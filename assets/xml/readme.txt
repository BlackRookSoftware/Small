Inside this directory are the unaltered XML files that need to be present in 
the root of /WEB-INF/ in order for SFDispatcherServlet and all of its extensions to
function. They are here in order to be packaged into End-User distributions 
of the library/framework via the build script or serve as backups.
